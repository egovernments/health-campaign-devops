apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: who-afro-chaduat-appset-frontend
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: workbench-ui
          - name: hcm-digit-ui
          - name: dashboard-ui
          - name: payments-ui
          - name: dss-dashboard
          - name: core-ui
          - name: console
          - name: microplan-ui

  template:
    metadata:
      name: 'chaduat-{{name}}'

    spec:
      project: chaduat-project

      source:
        repoURL: git@github.com:HCM-WHO-AFRO/health-campaign-devops.git
        targetRevision: azure-install-who-test
        path: config-as-code/helm/charts/frontend/{{name}}
        helm:
          valueFiles:
            - values.yaml
            - ../../../../environments/chaduat-afro-hcm.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: chaduat

      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas

      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
        retry:
          limit: 10
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m0s
