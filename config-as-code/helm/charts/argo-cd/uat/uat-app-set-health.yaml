apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: hcm-demo-uat-appset-health
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: transformer
          - name: health-expense-calculator

  template:
    metadata:
      name: 'uat-{{name}}'

    spec:
      project: uat-project

      source:
        repoURL: git@github.com:egovernments/health-campaign-devops.git
        targetRevision: azure-install-hcm-demo
        path: config-as-code/helm/charts/health-services/{{name}}
        helm:
          valueFiles:
            - values.yaml
            - ../../../../environments/uat-tenant.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: uat

      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas

      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
        retry:
          limit: 10
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m0s
