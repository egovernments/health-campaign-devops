# Common Labels
labels:
  app: "hcm-digit-ui"
  group: "web"

# Ingress Configs
ingress:
  enabled: true
  context: "hcm-digit-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "egovio/hcm-digit-ui"
  tag: "single-web-app-hcm-digit-ui-1ed9dac"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/"
  readinessProbePath: "/"

namespace: egov    
extraVolumes: |
  - name: js-injection
    configMap:
      name: hcm-digit-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
